node_modules/
dist/
bun.lock
*.log
.env
.env.local
.env.*
.DS_Store
*.tsbuildinfo
CLAUDE.md
*.dxt
test-live*.sh
test-live*.mjs
test-live*.js
# Local agent/config state that may hold credentials — never commit.
.claude/
settings.local.json
# Local secret/PII denylist (personal domains/addresses) — never commit.
.secret-scan-local.txt

# ---- Fork: local agent notes, never published ---------------------------
CLAUDE.local.md
*.local.md

# ---- Fork: CLAUDE.md is ours and IS published (overrides the line above) -
!CLAUDE.md

# ---- Fork: upstream check -----------------------------------------------
!.vscode/
!.vscode/tasks.json
UPSTREAM-UPDATE.md

# ---- Fork: env template is safe to publish -------------------------------
!.env.example
