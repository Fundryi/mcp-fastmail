# Tool reference

Generated by `node scripts/gen-tool-docs.mjs` from the server's own tool list. 91 tools. Do not edit by hand; rebuild and rerun the script.

A fork tool with the same name as an upstream tool replaces it and keeps every upstream argument. `official_*` tools appear only with `FASTMAIL_MCP_TOKEN` set and are not listed here.

## Contents

- [Listing and search](#listing-and-search)
- [Read](#read)
- [Email state](#email-state)
- [Bulk operations](#bulk-operations)
- [Create and update](#create-and-update)
- [Delete and trash](#delete-and-trash)
- [Sending](#sending)
- [Attachments](#attachments)
- [Account](#account)

## Listing and search

### `advanced_search`

Search emails with any mix of filters (all ANDed): free text, from/to/cc/bcc/subject/body substrings, toDomain, header presence or value, size, attachment, unread, pinned, date range, and mailbox scoping (mailboxId, requiredMailboxIds intersection, excludeMailboxIds, includeChildren to expand mailboxId and excludeMailboxIds to their descendants). Returns { total, position, items }. Read-only. cc/bcc/body/minSize/maxSize/header are RFC 8621 filter fields, not yet confirmed against live Fastmail.

  - `query` (string) Free text (subject, body, addresses).
  - `from` (string) Sender contains this text.
  - `to` (string) To header contains this text (substring, so "@example.com" matches a domain).
  - `cc` (string) Cc header contains this text.
  - `bcc` (string) Bcc header contains this text.
  - `subject` (string) Subject contains this text.
  - `body` (string) Body contains this text.
  - `toDomain` (string) Recipient domain, e.g. "example.com". Sent as to: "@example.com".
  - `header` (object) Header must exist ({name}) or contain value ({name, value}). JMAP header: [name] / [name, value].
  - `hasAttachment` (boolean)
  - `isUnread` (boolean)
  - `isPinned` (boolean) $flagged keyword.
  - `minSize` (number) Bytes, inclusive.
  - `maxSize` (number) Bytes, exclusive (JMAP maxSize).
  - `mailboxId` (string) Only this mailbox. With includeChildren: this mailbox or any descendant.
  - `requiredMailboxIds` (array) Member of ALL of these (intersection). Not expanded by includeChildren.
  - `excludeMailboxIds` (array) Member of NONE of these. Expanded by includeChildren.
  - `includeChildren` (boolean) Expand mailboxId and excludeMailboxIds to include every descendant mailbox (one extra Mailbox/get).
  - `after` (string) receivedAt >= this ISO 8601 date.
  - `before` (string) receivedAt < this ISO 8601 date.
  - `limit` (number | string) Max results (default 50, cap 500).
  - `position` (number) Offset for paging (default 0).
  - `ascending` (boolean) Oldest first (default newest first).
  - `fields` (array) Email properties to return. Default: id, threadId, mailboxIds, keywords, from, to, subject, receivedAt, preview, hasAttachment, size.

### `advanced_search_metadata`

Same filter capabilities as advanced_search (single-mailbox scoping via mailboxId, multi-mailbox intersection via requiredMailboxIds, exclusion via excludeMailboxIds, plus sender / recipient / subject / free text / date / attachment / unread / pinned) but returns ONLY metadata on each match — id, threadId, subject, from, to, cc, replyTo, receivedAt, hasAttachment, keywords. Does NOT return preview or any body-derived content. Use in privacy-sensitive flows where the routing decision is made from headers alone — for example, when classifying customer mail by sender / recipient / subject / thread state without ingesting body content. The free-text query still searches body content on the server side; only the result envelope comes back without body excerpts. When the server reports a total match count, results are wrapped in a {"total", "items"} JSON envelope; otherwise a bare JSON array is returned.

  - `query` (string) Text to search for in subject/body
  - `from` (string) Filter by sender email
  - `to` (string) Filter by recipient email
  - `subject` (string) Filter by subject
  - `hasAttachment` (boolean) Filter emails with attachments
  - `isUnread` (boolean) Filter unread emails
  - `isPinned` (boolean) Filter pinned emails
  - `mailboxId` (string) Search within a single mailbox. For an intersection across multiple mailboxes (e.g. Inbox AND a label folder), use requiredMailboxIds instead.
  - `requiredMailboxIds` (array) Require membership in ALL of these mailbox IDs (intersection / AND semantic). Use this for queries like "in Inbox AND a label folder" — pass both mailbox IDs in the array. If mailboxId is also passed, it is folded into the intersection (de-duplicated). JMAP cannot express multi-mailbox membership in a single FilterCondition, so this builds a FilterOperator AND over multiple inMailbox conditions on the server.
  - `excludeMailboxIds` (array) Exclude emails that are members of ANY of these mailbox IDs (maps to JMAP inMailboxOtherThan). Useful for queries like "in a parent label but not its archive sub-folder". Combines cleanly with mailboxId / requiredMailboxIds.
  - `after` (string) Emails after this date (ISO 8601)
  - `before` (string) Emails before this date (ISO 8601)
  - `limit` (number | string) Maximum results (default: 50)
  - `ascending` (boolean) Sort oldest first instead of newest first (default: false)

### `extract_codes`

Pull one-time codes (OTP, verification, 2FA) out of recent emails. Searches subject, preview and text body for pattern (regex, default 4 to 8 digit numbers) in emails received after `after` (default: 2 hours ago), optionally filtered by from/to/mailboxId. With the default pattern, years (1900-2099) and 10+ digit numbers are skipped. Returns [{ code, emailId, from, to, subject, receivedAt }], newest first, first match per email. Read-only.

  - `from` (string)
  - `to` (string)
  - `mailboxId` (string)
  - `after` (string) ISO 8601, default now minus 2 hours.
  - `limit` (number | string) Emails to scan (default 20, cap 500).
  - `pattern` (string) Regex for the code (default \b\d{4,8}\b). Group 1 is used when present.

### `find_duplicates`

Find duplicate emails among those matching the same filters as advanced_search, scanning up to maxEmails (default 2000). Groups by Message-ID header; when missing, by subject+from+sentAt. Returns only groups with 2+ members: [{ key, count, emails: [{ id, mailboxIds, receivedAt, size }] }]. Read-only; pair with a move/trash tool to act on the result.

  - `query` (string) Free text (subject, body, addresses).
  - `from` (string) Sender contains this text.
  - `to` (string) To header contains this text (substring, so "@example.com" matches a domain).
  - `cc` (string) Cc header contains this text.
  - `bcc` (string) Bcc header contains this text.
  - `subject` (string) Subject contains this text.
  - `body` (string) Body contains this text.
  - `toDomain` (string) Recipient domain, e.g. "example.com". Sent as to: "@example.com".
  - `header` (object) Header must exist ({name}) or contain value ({name, value}). JMAP header: [name] / [name, value].
  - `hasAttachment` (boolean)
  - `isUnread` (boolean)
  - `isPinned` (boolean) $flagged keyword.
  - `minSize` (number) Bytes, inclusive.
  - `maxSize` (number) Bytes, exclusive (JMAP maxSize).
  - `mailboxId` (string) Only this mailbox. With includeChildren: this mailbox or any descendant.
  - `requiredMailboxIds` (array) Member of ALL of these (intersection). Not expanded by includeChildren.
  - `excludeMailboxIds` (array) Member of NONE of these. Expanded by includeChildren.
  - `includeChildren` (boolean) Expand mailboxId and excludeMailboxIds to include every descendant mailbox (one extra Mailbox/get).
  - `after` (string) receivedAt >= this ISO 8601 date.
  - `before` (string) receivedAt < this ISO 8601 date.
  - `maxEmails` (number) Scan cap (default 2000).

### `list_address_books`

List the address books on the account (AddressBook/get, RFC 9610). Returns [{id, name, description, isSubscribed, isDefault, shareWith, myRights}] as the server gives them. Use it to see whether a shared company directory is a second address book. Refuses when the token lacks the contacts capability.

  No parameters.

### `list_aliases_with_usage`

Return each identity with mailCount and lastReceivedAt (null if none found), using one batched set of recipient Email/query calls. Counts are mail matching the identity email in To, not delivery logs. Refuses more than 100 identities. Refuses unavailable capabilities with the likely missing API token scope.

  No parameters.

### `list_attachments`

List attachments across emails in one flat array: [{ emailId, subject, from, receivedAt, attachmentId, name, type, size }]. attachmentId is the part id that get_email_attachments / download_attachment accept. Scope by mailboxId, path, or filter (same keys as advanced_search: query, from, to, subject, after, before, isUnread, ...); then narrow client-side by type (MIME prefix like "application/pdf" or "image/"), minSize/maxSize (bytes), nameContains. Inline parts (disposition "inline", typically embedded images) are skipped unless includeInline: true. limit is the number of emails scanned (default 100, max 500), newest first.

  - `mailboxId` (string) Restrict to a mailbox id.
  - `path` (string) Restrict to a folder path like "Parent/Child".
  - `filter` (object) advanced_search filter keys. hasAttachment is always forced to true.
  - `type` (string) MIME type prefix, e.g. "application/pdf" or "image/".
  - `minSize` (number) Minimum attachment size in bytes.
  - `maxSize` (number) Maximum attachment size in bytes.
  - `nameContains` (string) Case-insensitive substring of the file name.
  - `includeInline` (boolean) Also list inline parts. Default false.
  - `limit` (number) Emails to scan. Default 100, max 500.

### `list_calendar_events`

List events from a calendar

  - `calendarId` (string) ID of the calendar (optional, defaults to all calendars)
  - `startDate` (string) Filter events starting from this date (ISO 8601, e.g. 2026-03-23T00:00:00Z)
  - `endDate` (string) Filter events ending before this date (ISO 8601, e.g. 2026-03-30T00:00:00Z)
  - `limit` (number) Maximum number of events to return (default: 50)

### `list_calendars`

List all calendars

  No parameters.

### `list_contacts`

List contacts from the address book. When the server reports a total match count, results are wrapped in a {"total", "items"} JSON envelope; otherwise a bare JSON array is returned.

  - `limit` (number) Maximum number of contacts to return (default: 50)

### `list_emails`

List emails, newest first, optionally scoped to one mailbox. Returns { total, position, items }. Every item carries mailboxIds by default; pass fields to choose Email properties. Page with position. limit caps at 500. Read-only.

  - `mailboxId` (string) Only this mailbox (default: all).
  - `limit` (number | string) Max results (default 20, cap 500).
  - `position` (number) Offset for paging (default 0).
  - `ascending` (boolean) Oldest first (default newest first).
  - `fields` (array) Email properties to return. Default: id, threadId, mailboxIds, keywords, from, to, subject, receivedAt, preview, hasAttachment, size.

### `list_emails_metadata`

Same as list_emails (lists emails from a mailbox, optionally filtered by mailboxId, with paging and sort) but returns ONLY metadata fields on each result — id, threadId, subject, from, to, replyTo, receivedAt, hasAttachment, keywords. Does NOT return preview or any body-derived content. Use in privacy-sensitive flows where the workflow needs only the envelope (e.g. customer-mail least-privilege scans, or any caller forbidden from ingesting message bodies). Pair with get_email_metadata for follow-up lookups that should also stay header-only. When the server reports a total match count, results are wrapped in a {"total", "items"} JSON envelope; otherwise a bare JSON array is returned.

  - `mailboxId` (string) ID of the mailbox to list emails from (optional, defaults to all)
  - `limit` (number | string) Maximum number of emails to return (default: 20)
  - `ascending` (boolean) Sort oldest first instead of newest first (default: false)

### `list_identities`

List sending identities (email addresses that can be used for sending)

  No parameters.

### `list_mailboxes`

List mailboxes in the Fastmail account. By default returns all mailboxes with full metadata; on accounts with hundreds of mailboxes the full result can exceed the MCP tool result window. Use `properties: ["id","name","parentId"]` for a slim view, and/or `parentId` to filter to one level of children.

  - `properties` (array) JMAP Mailbox properties to return (e.g. ["id","name","parentId"]). Default: all properties. The slim form roughly halves payload size on large accounts.
  - `parentId` (string | null) Filter to direct children of this mailbox ID. Pass null for top-level mailboxes. Filter is applied client-side after Mailbox/get.

### `list_masked_emails`

List masked email records from the masked-email primary account. Refuses a missing primary account. Refuses unavailable capabilities with the likely missing API token scope.

  No parameters.

### `list_operations`

List the last 50 process-local bulk/undo operations as id, tool, at (ISO), count (successful updates), without email state. Does not read the audit file.

  No parameters.

### `list_scheduled`

List pending email submissions: scheduled sends and mail still inside the undo-send window. Returns [{ id, emailId, sendAt, undoStatus, subject, to }]. Cancel one with cancel_send.

  No parameters.

### `list_sieve_scripts`

List all Sieve scripts as id, name, isActive, and blobId. Refuses unavailable capabilities with the likely missing API token scope.

  No parameters.

### `list_unread_across`

Unread and total counts for a parent mailbox and every descendant, each with its full path, plus totalUnread. Name the parent by mailboxId or path ("Inbox/Clients"). withEmails: true also returns up to perMailboxLimit (default 10) unread email headers per mailbox, fetched in one batched request. Refuses when neither mailboxId nor path is given or the parent does not exist. Read-only.

  - `mailboxId` (string)
  - `path` (string) Slash-separated mailbox path, alternative to mailboxId.
  - `withEmails` (boolean)
  - `perMailboxLimit` (number) Unread headers per mailbox when withEmails (default 10, cap 500).

### `search_contacts`

Search contacts by name or email. When the server reports a total match count, results are wrapped in a {"total", "items"} JSON envelope; otherwise a bare JSON array is returned.

  - `query` (string, required) Search query string
  - `limit` (number) Maximum number of results (default: 20)

### `search_emails`

Search mail with a Gmail-style query string. Operators (case-insensitive, values may be quoted: subject:"steam guard"): from:, to:, cc:, bcc:, subject:, body:, has:attachment, is:unread, is:read, is:flagged (alias is:starred), is:unflagged, in:<folder path, mailbox id, or inbox|trash|spam|junk|sent|drafts|archive>, -in:<same> to exclude, after:YYYY-MM-DD, before:YYYY-MM-DD, newer_than:7d|2w|1m|1y, older_than:..., larger:10M|500k|1000, smaller:..., header:Name=value or header:Name, domain:example.com (recipient domain). Remaining words are free text over subject, body and addresses. in: includes child folders unless includeChildren is false. Returns {total, position, items, parsed}; parsed shows how the string was read. Refuses an unknown operator (a bare word with a colon, such as a URL, must be quoted), a malformed date, size or duration, and an unresolvable in: folder.

  - `query` (string, required) Gmail-style query, e.g. from:example.com in:inbox is:unread newer_than:7d
  - `limit` (number | string) Max results (default 20, cap 500).
  - `position` (number) Offset for paging (default 0).
  - `ascending` (boolean) Oldest first (default newest first).
  - `excludeDrafts` (boolean) Omit drafts ($draft keyword). Default false.
  - `includeChildren` (boolean) Expand in: and -in: to child folders. Default true.
  - `fields` (array) Email properties to return (default: id, threadId, mailboxIds, keywords, from, to, subject, receivedAt, preview, hasAttachment, size).

### `search_emails_metadata`

Same as search_emails (free-text search across subject and body) but returns ONLY metadata on each match — id, threadId, subject, from, to, replyTo, receivedAt, hasAttachment, keywords. The query still searches body text on the server side; only the result envelopes come back, never preview or body excerpts. Use when a content match is required (e.g. "find all messages mentioning X") but the matches must not surface body fragments to the caller. When the server reports a total match count, results are wrapped in a {"total", "items"} JSON envelope; otherwise a bare JSON array is returned.

  - `query` (string, required) Search query string
  - `limit` (number | string) Maximum number of results (default: 20)
  - `ascending` (boolean) Sort oldest first instead of newest first (default: false)

### `summarize_mailbox`

Aggregate statistics over emails matching the same filters as advanced_search. Pages through up to maxEmails (default 2000) fetching headers only. Returns { total, sampled, unread, unreadRatio, dateRange, byFrom, byTo, bySubject } with top-50 lists; addresses lowercased, subjects stripped of Re:/Fwd:/AW:/WG: and cut to 60 chars. Read-only.

  - `query` (string) Free text (subject, body, addresses).
  - `from` (string) Sender contains this text.
  - `to` (string) To header contains this text (substring, so "@example.com" matches a domain).
  - `cc` (string) Cc header contains this text.
  - `bcc` (string) Bcc header contains this text.
  - `subject` (string) Subject contains this text.
  - `body` (string) Body contains this text.
  - `toDomain` (string) Recipient domain, e.g. "example.com". Sent as to: "@example.com".
  - `header` (object) Header must exist ({name}) or contain value ({name, value}). JMAP header: [name] / [name, value].
  - `hasAttachment` (boolean)
  - `isUnread` (boolean)
  - `isPinned` (boolean) $flagged keyword.
  - `minSize` (number) Bytes, inclusive.
  - `maxSize` (number) Bytes, exclusive (JMAP maxSize).
  - `mailboxId` (string) Only this mailbox. With includeChildren: this mailbox or any descendant.
  - `requiredMailboxIds` (array) Member of ALL of these (intersection). Not expanded by includeChildren.
  - `excludeMailboxIds` (array) Member of NONE of these. Expanded by includeChildren.
  - `includeChildren` (boolean) Expand mailboxId and excludeMailboxIds to include every descendant mailbox (one extra Mailbox/get).
  - `after` (string) receivedAt >= this ISO 8601 date.
  - `before` (string) receivedAt < this ISO 8601 date.
  - `maxEmails` (number) Sample size cap (default 2000).

## Read

### `get_account_summary`

Return the upstream account summary plus capability URIs, identities, unique lowercase identity domains, catch-all domains, quota (null when unavailable), and warnings. Failures in added fields become warnings and preserve the upstream summary.

  No parameters.

### `get_calendar_event`

Get a specific calendar event by ID. Returns organizer and participants when available.

  - `eventId` (string, required) ID of the event to retrieve

### `get_changes`

Incremental sync. Without sinceState returns the current { mailbox, email } state strings. With sinceState: { mailbox?, email? } returns what changed since: { mailbox: { oldState, newState, hasMoreChanges, created, updated, destroyed, mailboxes }, email: { ..., createdEmails } }. createdEmails carries up to 100 new emails (id, mailboxIds, from, to, subject, receivedAt, keywords). Up to 500 changes per call; hasMoreChanges means call again with newState. If the server no longer knows the old state (cannotCalculateChanges) call again without sinceState and resync.

  - `sinceState` (object) State strings from a previous call. Omit to fetch current states.

### `get_contact`

Get a specific contact by ID

  - `contactId` (string, required) ID of the contact to retrieve

### `get_email`

Get a specific email by ID

  - `emailId` (string, required) ID of the email to retrieve

### `get_email_attachments`

Get list of attachments for an email

  - `emailId` (string, required) ID of the email

### `get_email_metadata`

Get headers/metadata for an email — sender, recipients, subject, date, threading, mailbox membership, keywords (read/flagged/etc.), size, and whether an attachment is present — but NOT the body, preview, or any rendered text. Useful when a workflow needs to classify or route an email without ingesting its content (e.g. customer-mail least-privilege flows where reading bodies is forbidden, or skills that only need to verify post-archive folder placement). The return shape is the standard JMAP Email object restricted to a strict header-only allowlist.

  - `emailId` (string, required) ID of the email to retrieve metadata for

### `get_mailbox`

Fetch one mailbox with every property Fastmail stores (no properties filter), including the Fastmail-only settings (identityRef, autoPurge, purgeOlderThanDays, learnAsSpam, autoLearn, hidden, isCollapsed, suppressDuplicates, sort). Returns the raw Mailbox object. Refuses when neither mailboxId nor path is given or the mailbox does not exist.

  - `mailboxId` (string) Mailbox id. Give this or `path`.
  - `path` (string) Full folder path, e.g. "Parent/Child". Give this or `mailboxId`.

### `get_mailbox_by_name`

Resolve a folder path to { id, name, parentId, path }. Every user folder lives under Inbox, so "Inbox/Parent/Child" is the full path; "Parent/Child" is accepted too and tried with the Inbox prefix. Refuses when no folder matches. Mailbox ids are stable: resolve once, then use the id.

  - `path` (string, required) Folder path, "/" separated.

### `get_mailbox_stats`

Get statistics for a mailbox (unread count, total emails, etc.)

  - `mailboxId` (string) ID of the mailbox (optional, defaults to all mailboxes)

### `get_quota`

Return all account quota records, including usage and server limits. Refuses unavailable capabilities with the likely missing API token scope.

  No parameters.

### `get_recent_emails`

Get the most recent emails across all mailboxes except Trash and Spam (pass mailboxName to scope to one folder, e.g. "inbox"). When the server reports a total match count, results are wrapped in a {"total", "items"} JSON envelope; otherwise a bare JSON array is returned.

  - `limit` (number | string) Number of recent emails to retrieve (default: 10, max: 50)
  - `mailboxName` (string) Mailbox to search (optional; when omitted, all mailboxes except Trash and Spam are searched)
  - `ascending` (boolean) Sort oldest first instead of newest first (default: false)

### `get_session`

Fetch the raw JMAP session and return username, apiUrl, primaryAccounts, the session capabilities and, per account, its name and accountCapabilities. Use it to see which JMAP extensions this API token can reach (sieve, blob, quota, maskedemail) before calling the tools that need them.

  No parameters.

### `get_sieve_script`

Get script metadata and full content by id or active: true (exactly one). Refuses a missing script. Refuses unavailable capabilities with the likely missing API token scope.

  - `id` (string)
  - `active` (boolean)

### `get_thread`

Every message in a conversation with its body. threadId may also be an email id (resolved to its thread). format: text (default; HTML-only messages are tag-stripped), html, or both. Each message: id, from, to, cc, subject, receivedAt, keywords, mailboxIds, hasAttachment, body (and html for html/both), truncated when maxBodyBytes (default 20000 per part) cut it. $draft messages are excluded unless includeDrafts. Refuses when the thread does not exist. Read-only.

  - `threadId` (string, required) Thread id, or an email id.
  - `includeDrafts` (boolean) Include $draft messages (default false).
  - `format` (string) Body format (default text).
  - `maxBodyBytes` (number) Max bytes per body part (default 20000).

### `get_thread_metadata`

Same as get_thread (enumerate every message in a conversation thread) but returns ONLY metadata on each thread message — id, threadId, subject, from, to, cc, replyTo, receivedAt, hasAttachment, keywords. Does NOT return preview or any body-derived content. Use for thread-state checks (reply-presence detection, sender enumeration, date comparison, read/flagged status) without ingesting message bodies — particularly in customer-mail least-privilege flows where the skill needs to know "did we reply, when, and from which alias" but is forbidden from reading what was said. Accepts either a thread ID or an email ID and resolves to the parent thread, mirroring get_thread.

  - `threadId` (string, required) ID of the thread/conversation (an email ID is also accepted and will be resolved to its parent thread)

### `get_vacation_response`

Return the vacation-response singleton and its current settings. Refuses unavailable capabilities with the likely missing API token scope.

  No parameters.

## Email state

### `add_labels`

Add labels (mailboxes) to an email without removing existing ones

  - `emailId` (string, required) ID of the email to add labels to
  - `mailboxIds` (array, required) Array of mailbox IDs to add as labels

### `archive_email`

Archive an email — move it to the target mailbox AND mark it as read in a single atomic JMAP operation. Equivalent to calling move_email followed by mark_email_read, but in one MCP call and one Email/set patch (the move and the read flag land together or not at all). For trashing an email, use delete_email instead — that follows a different convention and does not auto-mark-read.

  - `emailId` (string, required) ID of the email to archive
  - `targetMailboxId` (string, required) ID of the destination mailbox

### `import_email`

Import a local .eml file (RFC 5322 message) into a mailbox via Email/import. The path must resolve inside the download directory (FASTMAIL_DOWNLOAD_DIR, default ~/Downloads/fastmail-mcp/), same rule as attachments. Keywords default to { "$seen": true }. Returns the created email as stored: { id, blobId, threadId, size }.

  - `localPath` (string, required) Path to the .eml file, inside the download directory.
  - `mailboxId` (string) Target mailbox id. Give this or `path`.
  - `path` (string) Target folder path like "Parent/Child". Give this or `mailboxId`.
  - `keywords` (object) JMAP keywords map, e.g. { "$seen": true, "$flagged": true }. Default { "$seen": true }.
  - `receivedAt` (string) ISO 8601 UTC date to record as received. Default: server decides (usually Date header or now).

### `mark_email_read`

Mark an email as read or unread

  - `emailId` (string, required) ID of the email to mark
  - `read` (boolean) true to mark as read, false to mark as unread

### `move_email`

Move an email to a different mailbox

  - `emailId` (string, required) ID of the email to move
  - `targetMailboxId` (string, required) ID of the target mailbox

### `pin_email`

Pin or unpin an email

  - `emailId` (string, required) ID of the email to pin/unpin
  - `pinned` (boolean) true to pin, false to unpin

### `remove_labels`

Remove specific labels (mailboxes) from an email

  - `emailId` (string, required) ID of the email to remove labels from
  - `mailboxIds` (array, required) Array of mailbox IDs to remove as labels

### `report_not_spam`

Move emails out of Junk into the Inbox (role "inbox"), removing them from every other folder. Fastmail trains its spam filter on the move. Returns { mailboxId, updated, notUpdated }.

  - `emailIds` (array, required) Email ids to move.

### `report_spam`

Move emails into the Junk folder (role "junk"), removing them from every other folder. Fastmail trains its spam filter on the move. Returns { mailboxId, updated, notUpdated }.

  - `emailIds` (array, required) Email ids to move.

### `unsubscribe`

Unsubscribe from a mailing list via the List-Unsubscribe header (RFC 2369 / RFC 8058). Without confirm it only reports what the email offers: { oneClick, https, mailto, action: "none" }. With confirm: true and oneClick it POSTs "List-Unsubscribe=One-Click" to the first https URL (no auth, no redirects, 10 s timeout) and returns { action: "posted", url, status }. Refuses when the header is missing, when no https one-click endpoint exists (non-https URLs are never fetched), or when only a mailto address exists; in the mailto case send an email to the returned address with send_email instead. A plain https link without List-Unsubscribe-Post is not fetched either, because those pages often need a browser.

  - `emailId` (string, required) Email id.
  - `confirm` (boolean) Actually send the one-click POST. Default false: inspect only.

## Bulk operations

### `bulk_add_labels`

Add mailbox labels without changing other memberships. Select exactly one of emailIds, mailboxId (whole folder), or filter (empty means all mail). Queries use pages of 500; maxEmails defaults to 1000 and larger selections refuse until raised. dryRun returns count, first 200 emailIds and action without writes or logging. Above FASTMAIL_BULK_CONFIRM_THRESHOLD (default 100), require confirm:true. Returns operationId, requested/updated counts, per-id notUpdated, action and storedSample (mailboxIds/keywords of up to 20 emails read back from the server); auditLogError if file logging failed. Undo is available for the last 50 operations of this process via undo_operation.

  - `emailIds` (array)
  - `mailboxId` (string) Select every email in this mailbox.
  - `filter` (object)
  - `maxEmails` (integer)
  - `dryRun` (boolean)
  - `confirm` (boolean)
  - `mailboxIds` (array, required)

### `bulk_delete`

Move emails to the mailbox with role trash; never permanently delete. Refuses if no trash role exists. Select exactly one of emailIds, mailboxId (whole folder), or filter (empty means all mail). Queries use pages of 500; maxEmails defaults to 1000 and larger selections refuse until raised. dryRun returns count, first 200 emailIds and action without writes or logging. Above FASTMAIL_BULK_CONFIRM_THRESHOLD (default 100), require confirm:true. Returns operationId, requested/updated counts, per-id notUpdated, action and storedSample (mailboxIds/keywords of up to 20 emails read back from the server); auditLogError if file logging failed. Undo is available for the last 50 operations of this process via undo_operation.

  - `emailIds` (array)
  - `mailboxId` (string) Select every email in this mailbox.
  - `filter` (object)
  - `maxEmails` (integer)
  - `dryRun` (boolean)
  - `confirm` (boolean)

### `bulk_mark_read`

Mark emails read or unread. Select exactly one of emailIds, mailboxId (whole folder), or filter (empty means all mail). Queries use pages of 500; maxEmails defaults to 1000 and larger selections refuse until raised. dryRun returns count, first 200 emailIds and action without writes or logging. Above FASTMAIL_BULK_CONFIRM_THRESHOLD (default 100), require confirm:true. Returns operationId, requested/updated counts, per-id notUpdated, action and storedSample (mailboxIds/keywords of up to 20 emails read back from the server); auditLogError if file logging failed. Undo is available for the last 50 operations of this process via undo_operation.

  - `emailIds` (array)
  - `mailboxId` (string) Select every email in this mailbox.
  - `filter` (object)
  - `maxEmails` (integer)
  - `dryRun` (boolean)
  - `confirm` (boolean)
  - `read` (boolean)

### `bulk_move`

Replace all mailbox memberships with the target mailbox. Select exactly one of emailIds, mailboxId (whole folder), or filter (empty means all mail). Queries use pages of 500; maxEmails defaults to 1000 and larger selections refuse until raised. dryRun returns count, first 200 emailIds and action without writes or logging. Above FASTMAIL_BULK_CONFIRM_THRESHOLD (default 100), require confirm:true. Returns operationId, requested/updated counts, per-id notUpdated, action and storedSample (mailboxIds/keywords of up to 20 emails read back from the server); auditLogError if file logging failed. Undo is available for the last 50 operations of this process via undo_operation.

  - `emailIds` (array)
  - `mailboxId` (string) Select every email in this mailbox.
  - `filter` (object)
  - `maxEmails` (integer)
  - `dryRun` (boolean)
  - `confirm` (boolean)
  - `targetMailboxId` (string, required)

### `bulk_pin`

Pin or unpin emails. Select exactly one of emailIds, mailboxId (whole folder), or filter (empty means all mail). Queries use pages of 500; maxEmails defaults to 1000 and larger selections refuse until raised. dryRun returns count, first 200 emailIds and action without writes or logging. Above FASTMAIL_BULK_CONFIRM_THRESHOLD (default 100), require confirm:true. Returns operationId, requested/updated counts, per-id notUpdated, action and storedSample (mailboxIds/keywords of up to 20 emails read back from the server); auditLogError if file logging failed. Undo is available for the last 50 operations of this process via undo_operation.

  - `emailIds` (array)
  - `mailboxId` (string) Select every email in this mailbox.
  - `filter` (object)
  - `maxEmails` (integer)
  - `dryRun` (boolean)
  - `confirm` (boolean)
  - `pinned` (boolean)

### `bulk_remove_labels`

Remove only the specified mailbox labels. Select exactly one of emailIds, mailboxId (whole folder), or filter (empty means all mail). Queries use pages of 500; maxEmails defaults to 1000 and larger selections refuse until raised. dryRun returns count, first 200 emailIds and action without writes or logging. Above FASTMAIL_BULK_CONFIRM_THRESHOLD (default 100), require confirm:true. Returns operationId, requested/updated counts, per-id notUpdated, action and storedSample (mailboxIds/keywords of up to 20 emails read back from the server); auditLogError if file logging failed. Undo is available for the last 50 operations of this process via undo_operation.

  - `emailIds` (array)
  - `mailboxId` (string) Select every email in this mailbox.
  - `filter` (object)
  - `maxEmails` (integer)
  - `dryRun` (boolean)
  - `confirm` (boolean)
  - `mailboxIds` (array, required)

### `bulk_update_mailboxes`

Apply one patch (isSubscribed and/or sortOrder) to many folders at once. Scope with mailboxIds, or with parentId / parentPath for every folder below that parent (includeChildren: false limits it to direct children; the parent itself is never touched). System folders (any with a role) are skipped, and so are folders already at the requested value. dryRun: true returns the plan without writing. Returns { count, changed: [{ id, path, before, after }], skipped: [{ id, path, reason }] } with the values re-fetched after the write.

  - `mailboxIds` (array) Explicit folder ids. Give this or parentId / parentPath.
  - `parentId` (string) Patch the folders below this id.
  - `parentPath` (string) Patch the folders below this path, e.g. "Parent/Child".
  - `includeChildren` (boolean) With a parent: true (default) takes the whole subtree, false only direct children.
  - `isSubscribed` (boolean)
  - `sortOrder` (number)
  - `dryRun` (boolean) Report the plan, write nothing.

### `export_operation_log`

Return the full process-local operation ring as JSON, including before-state for undo. Optional operationId returns one entry; refuses an unknown/expired id. Does not read the audit file.

  - `operationId` (string)

### `undo_operation`

Restore mailboxIds and keywords from a retained operation using full-property sets; overwrites later changes to those fields. Refuses unknown/expired operationId. dryRun previews count/ids without writing. Returns restored count, notUpdated, stored read-back state and the undo operationId; auditLogError reports optional file-log failure. The undo is itself undoable.

  - `operationId` (string, required)
  - `dryRun` (boolean)

## Create and update

### `create_calendar_event`

Create a new calendar event. Supports date-only (e.g. 2026-04-01) for all-day events. DTEND is exclusive per RFC 5545 — a one-day event on April 1 needs end: 2026-04-02.

  - `calendarId` (string, required) ID of the calendar to create the event in
  - `title` (string, required) Event title
  - `description` (string) Event description (optional)
  - `start` (string, required) Start time in ISO 8601 format (e.g. 2026-04-07T14:00:00Z) or date-only for all-day events (e.g. 2026-04-07)
  - `end` (string, required) End time in ISO 8601 format. For all-day events, DTEND is exclusive — a one-day event on April 1 requires end: 2026-04-02
  - `location` (string) Event location (optional)
  - `participants` (array) Event participants (optional). Automatically adds ORGANIZER from CalDAV username.

### `create_contact`

Create a new contact in the address book. Requires a name or at least one email address. Requires an API token with read-write contacts scope.

  - `name` (object) Structured name; provide full and/or given/surname
  - `emails` (array) Email addresses (replaces ALL existing emails on update; [] clears)
  - `phones` (array) Phone numbers (replaces ALL existing phones on update)
  - `addresses` (array) Postal addresses as free-form text (replaces ALL existing on update)
  - `notes` (string) Free-form note (replaces the existing note on update)
  - `addressBookId` (string) Target address book id (default book when omitted)

### `create_draft`

Create an email draft without sending it. Supports threading headers for replies. IMPORTANT: each call creates a new draft — do not call twice for the same message.

  - `to` (array) Recipient email addresses (optional)
  - `cc` (array) CC email addresses (optional)
  - `bcc` (array) BCC email addresses (optional)
  - `from` (string) Sender email address (optional, defaults to account primary email)
  - `mailboxId` (string) Mailbox ID to save the draft to (optional, defaults to Drafts folder)
  - `subject` (string) Email subject (optional)
  - `textBody` (string) Plain text body (optional)
  - `htmlBody` (string) HTML body (optional)
  - `inReplyTo` (array) Message-IDs to reply to (optional, for threading)
  - `references` (array) Message-IDs for References header (optional, for threading)
  - `replyTo` (array) Reply-To email addresses (replies go here instead of to the sender)
  - `attachments` (array) Files to attach. Each entry must use EXACTLY ONE source: localPath (a file inside the configured download directory), emailId + attachmentId (re-attach an attachment from an existing email — no bytes are copied), or blobId (an already-uploaded JMAP blob). Optional name/type override the inferred filename and MIME type.

### `create_identity`

Create a sending identity with name, email, and optional replyTo/bcc address arrays and signatures. Returns the re-fetched identity; server restrictions on identity creation still apply. Refuses unavailable capabilities with the likely missing API token scope.

  - `name` (string, required)
  - `email` (string, required)
  - `replyTo` (array | null)
  - `bcc` (array | null)
  - `textSignature` (string)
  - `htmlSignature` (string)

### `create_mailbox`

Create a mailbox (folder). Sends name, parentId (null = top level), isSubscribed (default true, like the web app; over raw JMAP it would default to false and the folder would be hidden in IMAP clients), sortOrder and any raw properties in `extra`, then re-fetches and returns the stored Mailbox object as the server has it. Refuses a name containing "/". Live check 2026-09: with an API token Fastmail accepts only name, parentId, isSubscribed and sortOrder. Folder colour, the identity link (identityRef), autoPurge, learnAsSpam, isCollapsed and the other Fastmail-only fields are read-only over JMAP; set them in the web app. get_mailbox still shows them.

  - `name` (string, required) Leaf name. Must not contain "/".
  - `parentId` (string | null) Parent mailbox id, null for top level.
  - `isSubscribed` (boolean)
  - `sortOrder` (number)
  - `extra` (object) Raw Mailbox properties passed through untouched.

### `create_masked_email`

Create a masked address and return its re-fetched record. state defaults to enabled. forDomain is the site using the address, not the generated email domain; emailPrefix requests the generated local-part prefix. Uses the masked-email primary account and refuses if it is missing. Refuses unavailable capabilities with the likely missing API token scope.

  - `state` (string)
  - `description` (string)
  - `forDomain` (string)
  - `emailPrefix` (string)

### `edit_draft`

Edit an existing draft email. Since JMAP emails are immutable, this atomically destroys the old draft and creates a new one with the updated fields. Only fields you provide will be changed; others are preserved from the original draft.

  - `emailId` (string, required) The ID of the draft email to edit
  - `to` (array) Updated recipient email addresses (optional, keeps existing if omitted)
  - `cc` (array) Updated CC email addresses (optional)
  - `bcc` (array) Updated BCC email addresses (optional)
  - `from` (string) Updated sender email address (optional)
  - `subject` (string) Updated email subject (optional)
  - `textBody` (string) Updated plain text body (optional)
  - `htmlBody` (string) Updated HTML body (optional)
  - `replyTo` (array) Reply-To email addresses (replies go here instead of to the sender)
  - `attachments` (array) Files to attach. Each entry must use EXACTLY ONE source: localPath (a file inside the configured download directory), emailId + attachmentId (re-attach an attachment from an existing email — no bytes are copied), or blobId (an already-uploaded JMAP blob). Optional name/type override the inferred filename and MIME type.

### `merge_mailbox`

Move every email from source into target, then permanently destroy the now-empty source folder. Requires confirm: true. Refuses when source is a system folder (has a role), has children, or equals target. dryRun: true returns the plan without writing. Returns { destroyed, movedEmails, source, target }.

  - `source` (string, required) Mailbox id or path to empty and destroy.
  - `target` (string, required) Mailbox id or path that receives the mail.
  - `dryRun` (boolean) Report the plan, write nothing.
  - `confirm` (boolean) Must be true. The destroy of source is permanent.

### `update_calendar_event`

Update an existing calendar event. Preserves all existing data (attendees, reminders, recurrence rules, etc.) not being changed. Omit a field to leave it unchanged; passing an empty/whitespace string for title, description, or location is rejected (use clearFields to delete description/location). Floating times preserve the original timezone; explicit UTC/offset times convert to UTC. WARNING: providing participants replaces ALL existing attendee data (acceptance status, roles, etc.). participants: [] removes all attendees.

  - `eventId` (string, required) ID of the event to update
  - `title` (string) New event title
  - `description` (string) New event description
  - `start` (string) New start time in ISO 8601 format. Floating times (no Z/offset) preserve original timezone
  - `end` (string) New end time in ISO 8601 format. DTEND is exclusive per RFC 5545
  - `location` (string) New event location
  - `participants` (array) Replaces ALL existing attendees. Empty array removes all attendees. Omit to preserve existing attendees.
  - `clearFields` (array) Property names to delete from the event. Allowed: description, location. Cannot also pass the same field as a value.
  - `confirmRecurring` (boolean) Required when changing start/end on a recurring event with exceptions. Acknowledges that orphaned exception overrides will be removed.

### `update_contact`

Update an existing contact. Each provided field WHOLLY REPLACES the stored value (e.g. emails: [] removes all emails) — unspecified fields are left untouched. Requires read-write contacts scope.

  - `contactId` (string, required) ID of the contact to update
  - `name` (object) Structured name; provide full and/or given/surname
  - `emails` (array) Email addresses (replaces ALL existing emails on update; [] clears)
  - `phones` (array) Phone numbers (replaces ALL existing phones on update)
  - `addresses` (array) Postal addresses as free-form text (replaces ALL existing on update)
  - `notes` (string) Free-form note (replaces the existing note on update)
  - `expectState` (string) Optional JMAP state precondition (ifInState); update fails with stateMismatch if contacts changed since this state

### `update_identity`

Patch only supplied identity fields and return the re-fetched identity. Refuses an empty patch. Server restrictions on editable identity fields still apply. Refuses unavailable capabilities with the likely missing API token scope.

  - `identityId` (string, required)
  - `name` (string)
  - `email` (string)
  - `replyTo` (array | null)
  - `bcc` (array | null)
  - `textSignature` (string)
  - `htmlSignature` (string)

### `update_mailbox`

Patch one mailbox: name, parentId (null = top level), isSubscribed, sortOrder, or raw properties in `extra`. Only the fields given are sent. Returns the re-fetched stored Mailbox object. Refuses when no field is given, when name contains "/", or when parentId is the mailbox itself or one of its descendants. Live check 2026-09: with an API token Fastmail accepts only name, parentId, isSubscribed and sortOrder. Folder colour, the identity link (identityRef), autoPurge, learnAsSpam, isCollapsed and the other Fastmail-only fields are read-only over JMAP; set them in the web app. get_mailbox still shows them.

  - `mailboxId` (string) Mailbox id. Give this or `path`.
  - `path` (string) Full folder path, e.g. "Parent/Child". Give this or `mailboxId`.
  - `name` (string) Leaf name. Must not contain "/".
  - `parentId` (string | null) Parent mailbox id, null for top level.
  - `isSubscribed` (boolean)
  - `sortOrder` (number)
  - `extra` (object) Raw Mailbox properties passed through untouched.

### `update_masked_email`

Patch supplied state, description, or forDomain and return the re-fetched masked address. enabled receives normally; disabled sends new mail to Trash; deleted bounces new mail. Does not destroy the record. Refuses an empty patch or missing masked-email primary account. Refuses unavailable capabilities with the likely missing API token scope.

  - `id` (string, required)
  - `state` (string)
  - `description` (string)
  - `forDomain` (string)

## Delete and trash

### `delete_calendar_event`

Delete a calendar event by ID

  - `eventId` (string, required) ID of the event to delete

### `delete_contact`

Permanently delete a contact from the address book. This cannot be undone. Requires read-write contacts scope.

  - `contactId` (string, required) ID of the contact to delete
  - `expectState` (string) Optional JMAP state precondition (ifInState)

### `delete_email`

Delete an email (move to trash)

  - `emailId` (string, required) ID of the email to delete

### `delete_mailbox`

Permanently destroy a mailbox (folder). Requires confirm: true. Refuses system folders (any with a role) and folders that still have children. When the folder holds mail: with moveContentsTo (id or path) every email is moved there first; without it the call refuses unless onDestroyRemoveEmails: true is passed explicitly, and then emails whose ONLY folder is this one are destroyed permanently (not trashed, cannot be undone). dryRun: true returns the plan (counts, what would move, what would be destroyed) without writing. Returns { destroyed, movedEmails, name, path }.

  - `mailboxId` (string) Mailbox id. Give this or `path`.
  - `path` (string) Full folder path, e.g. "Parent/Child". Give this or `mailboxId`.
  - `moveContentsTo` (string) Mailbox id or path to move all emails into before destroying.
  - `onDestroyRemoveEmails` (boolean) Permanently destroy emails whose only folder is this one. Default false.
  - `dryRun` (boolean) Report the plan, write nothing.
  - `confirm` (boolean) Must be true. The destroy is permanent.

### `empty_mailbox`

PERMANENTLY destroy every email in Trash or Junk. This cannot be undone; the mail is gone, not moved. Only role "trash" or "junk" is accepted. Requires confirm: true. Use dryRun: true first: it returns { total, sample } with the first 20 ids, subjects and dates and destroys nothing. olderThan (ISO date) limits the destroy to mail received before that time. Stops after maxEmails (default 5000) per call. Mail that also sits in another folder is skipped and listed in skippedInOtherFolders. Returns { destroyed, notDestroyed, skippedInOtherFolders, total }.

  - `role` (string, required) Which system folder to empty.
  - `confirm` (boolean) Must be true. The destroy is permanent.
  - `dryRun` (boolean) Count and sample only, destroy nothing.
  - `olderThan` (string) ISO 8601 date. Only destroy mail received before it.
  - `maxEmails` (number) Cap per call. Default 5000.

## Sending

### `cancel_send`

Cancel a pending submission (a scheduled send, or a just-sent mail still in the undo window). Sets undoStatus to canceled and returns the submission as re-fetched. Fails with a JMAP error once the mail has left the server. The email itself stays where it is.

  - `submissionId` (string, required)

### `forward_email`

Forward an email as plain text: your comment, a "Forwarded message" header block, then the original text (HTML-only mail is stripped to text). Attachments are re-attached by blob reference unless includeAttachments=false. Subject gets "Fwd: ". Identity and sendAt as in send_email. Returns the same shape as send_email. Scheduling relies on the submission capability being visible in the session; when it is not, the send is attempted and the server decides.

  - `emailId` (string, required)
  - `to` (one of, required) Recipient addresses (array, or comma-separated string)
  - `cc` (one of) CC addresses (array, or comma-separated string)
  - `bcc` (one of) BCC addresses (array, or comma-separated string)
  - `comment` (string) Text placed above the forwarded block.
  - `includeAttachments` (boolean) Default true.
  - `identityId` (string) Sending identity id (see list_identities). Wins over fromEmail.
  - `fromEmail` (string) Send as this verified identity address. Refused if no identity matches; the error lists the available ones.
  - `from` (string) Alias of fromEmail (upstream name).
  - `sendAt` (string) ISO 8601 time to send at instead of now (SMTP FUTURERELEASE). Fastmail parks the mail in its Scheduled folder until then; cancel with cancel_send. Refused when the plan has no Scheduled Send.

### `reply_email`

Reply to an email with In-Reply-To / References threading and a "Re:" subject. The From identity is the one the original was addressed to (so a mail sent to an alias is answered from that alias), unless identityId or fromEmail says otherwise. replyAll=true adds the original To/Cc minus your own identities. send=false saves a draft instead. Returns the same shape as send_email (or { emailId, draft: true }). Scheduling relies on the submission capability being visible in the session; when it is not, the send is attempted and the server decides.

  - `originalEmailId` (string, required)
  - `to` (one of) Recipient (default: original sender) addresses (array, or comma-separated string)
  - `cc` (one of) CC addresses (array, or comma-separated string)
  - `bcc` (one of) BCC addresses (array, or comma-separated string)
  - `replyTo` (one of) Reply-To addresses (array, or comma-separated string)
  - `replyAll` (boolean) Also reply to every original To/Cc address (default false).
  - `textBody` (string)
  - `htmlBody` (string)
  - `send` (boolean | string) Send now (default true); false saves a draft.
  - `attachments` (array) Files to attach. Each entry uses EXACTLY ONE source: localPath (inside FASTMAIL_DOWNLOAD_DIR), emailId + attachmentId (re-use an existing attachment, no bytes copied), or blobId. Optional name/type override.
  - `identityId` (string) Sending identity id (see list_identities). Wins over fromEmail.
  - `fromEmail` (string) Send as this verified identity address. Refused if no identity matches; the error lists the available ones.
  - `from` (string) Alias of fromEmail (upstream name).
  - `sendAt` (string) ISO 8601 time to send at instead of now (SMTP FUTURERELEASE). Fastmail parks the mail in its Scheduled folder until then; cancel with cancel_send. Refused when the plan has no Scheduled Send.

### `send_draft`

Send an existing draft email. The draft must have recipients (to/cc/bcc) and a from address. After sending, the email is moved to the Sent folder and the draft keyword is removed.

  - `emailId` (string, required) The ID of the draft email to send

### `send_email`

Send an email now or at sendAt. Picks the From identity by identityId, then fromEmail, then the account default. Returns { emailId, submissionId, identity, sendAt, undoStatus } as stored by the server. Refuses an unknown fromEmail (lists the available identities), a sendAt in the past, and a missing body. Scheduling relies on the submission capability being visible in the session; when it is not, the send is attempted and the server decides.

  - `to` (one of, required) Recipient addresses (array, or comma-separated string)
  - `cc` (one of) CC addresses (array, or comma-separated string)
  - `bcc` (one of) BCC addresses (array, or comma-separated string)
  - `replyTo` (one of) Reply-To addresses (array, or comma-separated string)
  - `subject` (string, required)
  - `textBody` (string)
  - `htmlBody` (string)
  - `mailboxId` (string) Mailbox to create the draft in before sending (default Drafts).
  - `inReplyTo` (array)
  - `references` (array)
  - `attachments` (array) Files to attach. Each entry uses EXACTLY ONE source: localPath (inside FASTMAIL_DOWNLOAD_DIR), emailId + attachmentId (re-use an existing attachment, no bytes copied), or blobId. Optional name/type override.
  - `identityId` (string) Sending identity id (see list_identities). Wins over fromEmail.
  - `fromEmail` (string) Send as this verified identity address. Refused if no identity matches; the error lists the available ones.
  - `from` (string) Alias of fromEmail (upstream name).
  - `sendAt` (string) ISO 8601 time to send at instead of now (SMTP FUTURERELEASE). Fastmail parks the mail in its Scheduled folder until then; cancel with cancel_send. Refused when the plan has no Scheduled Send.

### `snooze_email`

Snooze an email until an ISO 8601 time: moves it from Inbox to the Snoozed folder and the server brings it back to moveToMailboxId (default Inbox) at `until`. Returns { id, mailboxIds, snoozed } as re-fetched. Refuses when the account has no mailbox with role "snoozed". The `snoozed` property shape ({ until, moveToMailboxId }) follows draft-ietf-jmap-snooze.

  - `emailId` (string, required)
  - `until` (string, required) ISO 8601 time.
  - `moveToMailboxId` (string) Where the mail reappears (default: Inbox).

### `unsnooze_email`

Cancel a snooze: clears `snoozed`, puts the email back in Inbox and removes it from the Snoozed folder. Returns { id, mailboxIds, snoozed } as re-fetched. Refuses when the account has no mailbox with role "snoozed". Follows draft-ietf-jmap-snooze.

  - `emailId` (string, required)

## Attachments

### `download_attachment`

Download an email attachment. If savePath is provided, saves the file to disk and returns the file path and size. Otherwise returns a download URL.

  - `emailId` (string, required) ID of the email
  - `attachmentId` (string, required) ID of the attachment
  - `savePath` (string) File path to save the attachment to. May be absolute or relative; relative paths resolve against ~/Downloads/fastmail-mcp/ (configurable via FASTMAIL_DOWNLOAD_DIR), so a bare filename lands there in one step. Absolute paths must fall within that directory; traversal or symlink escape outside it is rejected for security. To save directly into your own location, set FASTMAIL_DOWNLOAD_DIR to that root. Parent directories will be created automatically.

### `save_attachment_to_webdav`

Save an email attachment directly to WebDAV cloud storage (e.g. Fastmail Files or Nextcloud) without touching local disk. The storage server and credentials come from server configuration (FASTMAIL_WEBDAV_URL / _USERNAME / _PASSWORD); this tool only chooses the relative path beneath that base. Fails if the remote file exists unless overwrite is set.

  - `emailId` (string, required) ID of the email
  - `attachmentId` (string, required) Attachment partId, blobId, or zero-based index
  - `remotePath` (string, required) Relative path under the configured WebDAV base (e.g. "invoices/2026/receipt.pdf"). No leading slash, no "..", forward slashes only. Missing parent folders are created unless createParents is false.
  - `overwrite` (boolean) Replace an existing remote file (default false: fail if it exists)
  - `createParents` (boolean) Create missing parent collections via MKCOL (default true)

## Account

### `check_function_availability`

Check which MCP functions are available based on account permissions. Calendar tools run over CalDAV, so calendar is reported available when CalDAV credentials are configured, regardless of the JMAP calendar capability.

  No parameters.

### `set_sieve_script`

Create or overwrite a named Sieve script. Defaults to name mcp and activate false. Overwriting or activating requires confirm: true. Reads a complete active-script backup before writing and returns stored id/name/isActive plus previousActive { id, name, content }, or null. dryRun uploads a temporary blob and validates only, returning the proposed action and confirmation requirement. Updating an already active script leaves it active. Refuses an unreadable backup or a concurrent script change. Fastmail keeps the rules from its web UI as generated blocks inside the active script: read it first with get_sieve_script and keep those blocks byte for byte, or the UI rules break. Refuses unavailable capabilities with the likely missing API token scope.

  - `content` (string, required)
  - `name` (string)
  - `activate` (boolean)
  - `confirm` (boolean)
  - `dryRun` (boolean)

### `set_vacation_response`

Patch only supplied vacation settings on singleton and return the re-fetched response. Use null to clear dates or text fields. Refuses an empty patch. Refuses unavailable capabilities with the likely missing API token scope.

  - `isEnabled` (boolean)
  - `fromDate` (string | null)
  - `toDate` (string | null)
  - `subject` (string | null)
  - `textBody` (string | null)
  - `htmlBody` (string | null)

### `test_bulk_operations`

Test bulk operations by finding recent emails and performing safe operations (mark read/unread)

  - `dryRun` (boolean) If true, only shows what would be done without making changes (default: true)
  - `limit` (number) Number of emails to test with (default: 3, max: 10)

### `validate_sieve`

Validate Sieve content and return { valid, error }. Uploads a temporary blob for RFC validation, without creating or activating a script. Refuses unavailable capabilities with the likely missing API token scope.

  - `content` (string, required)

