# Store and check out every text file with LF. Without this, contributors on
# Windows produce line-ending-only diffs that look like real changes, and an
# upstream merge turns into a whole-file conflict.
* text=auto eol=lf
